export default class Menu {
  constructor (id, name, description, dateStart, dateFinish, available) {
    this.id = id
    this.name = name
    this.description = description
    this.dateStart = dateStart
    this.dateFinish = dateFinish
    this.available = available
  }
}
